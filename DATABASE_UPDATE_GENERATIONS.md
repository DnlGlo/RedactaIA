-- Tabla de Generaciones
create table generations (
  id bigint generated by default as identity primary key,
  user_email text,
  content_type text,
  created_at timestamp with time zone default now()
);

-- Habilitar acceso público
alter table generations enable row level security;
create policy "Public access" on generations for all using (true);

-- (Opcional) Trigger para mantener last_login actualizado
-- No es estrictamente necesario para lo que pides, pero es buena práctica.
